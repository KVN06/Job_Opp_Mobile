<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    
    <!-- Fondo redondeado oscuro -->
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="40dp" />
            <solid android:color="@color/splash_container_bg" />
            <stroke
                android:width="2dp"
                android:color="@color/splash_border" />
        </shape>
    </item>
    
    <!-- Logo centrado con menos padding para que se vea más grande -->
    <item
        android:drawable="@drawable/splash_logo"
        android:top="12dp"
        android:bottom="12dp"
        android:left="12dp"
        android:right="12dp" />
        
</layer-list>